# AI Handoff Instructions

## 🤖 How to use this context
If you are an AI reading this, **read all files in the `ai-context/` directory first** before writing any code. 

## 🎯 Your Immediate Task
We are resuming development at **Phase 5: Room Inventory Module**.

### Step 1: Room Types
Build the Room Type module (`src/modules/room-type/`).
- Create `room-type.validation.ts` (Zod schemas).
- Create `room-type.repository.ts` (Prisma queries, strictly filtered by `tenantId` via `propertyId`).
- Create `room-type.service.ts` (Business logic, error handling).
- Create `room-type.controller.ts` (HTTP handlers).
- Create `room-type.routes.ts` (Express router, attach auth/validate middleware).

### Step 2: Rooms & Rate Plans
Repeat the exact same layered pattern for `room` and `rate-plan` modules.

### Step 3: Availability Logic
Implement the availability check in the Room service. It must query existing reservations and exclude rooms that are booked between the requested `checkIn` and `checkOut` dates.

## ⚠️ Crucial Reminders
1. **DO NOT** break the Controller -> Service -> Repository pattern.
2. **DO NOT** hard delete records. Use `isActive: false`.
3. **DO** ensure every DB query is scoped to the user's `tenantId` (extracted from the JWT payload in the controller and passed down).
4. **DO** use Zod for all request validation.
5. **DO** write clean, strictly typed TypeScript. No `any`.

Once you understand these instructions, reply with: *"Context loaded. Ready to build Phase 5: Room Types."* and wait for my command to start coding.