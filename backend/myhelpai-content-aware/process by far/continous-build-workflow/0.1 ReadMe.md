# Starting a New AI Session

When you start a new chat with an AI (or start a new session), upload the entire `ai-context` folder and the `prisma` folder, and use the following prompt:

```text
Read the ai-context folder. We are continuing the Hotel PMS backend. Start with Phase 5: Room Types.
```
